import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { ArrowLeft, UserRoundPlus } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export const Route = createFileRoute("/register")({ head: () => ({ meta: [{ title: "Register — RoadWatch" }, { name: "description", content: "Create a RoadWatch account to report and track damaged roads." }, { property: "og:title", content: "Register — RoadWatch" }, { property: "og:description", content: "Create a RoadWatch account to report and track damaged roads." }] }), component: RegisterPage });
function RegisterPage() { const navigate = useNavigate(); const [form, setForm] = useState({ name: "", email: "", password: "", confirm: "" }); const [message, setMessage] = useState(""); const submit = (event: React.FormEvent) => { event.preventDefault(); if (Object.values(form).some((value) => !value)) { setMessage("Complete every field to create your account."); return; } if (form.password !== form.confirm) { setMessage("Passwords do not match."); return; } navigate({ to: "/dashboard" }); }; return <div className="grid min-h-screen place-items-center bg-cream px-4 py-10"><div className="w-full max-w-md"><Link to="/" className="mb-8 inline-flex items-center gap-2 font-mono text-[10px] uppercase tracking-[.1em] text-ink/55 hover:text-ink"><ArrowLeft className="size-3.5" />Back to RoadWatch</Link><div className="border-2 border-ink bg-paper p-6 shadow-[6px_6px_0_0_var(--ink)] sm:p-8"><div className="grid size-10 place-items-center rounded-md bg-brand text-paper shadow-[3px_3px_0_0_var(--ink)]"><UserRoundPlus className="size-5" /></div><h1 className="mt-5 text-3xl font-semibold tracking-tight">Join the field log.</h1><p className="mt-2 text-sm text-ink/60">Create an account to report issues and follow the fix.</p><form onSubmit={submit} className="mt-7 space-y-4">{[["name", "Name", "Your name", "text"], ["email", "Email", "you@example.com", "email"], ["password", "Password", "••••••••", "password"], ["confirm", "Confirm password", "••••••••", "password"]].map(([key, label, placeholder, type]) => <div key={key}><label className="field-label" htmlFor={key}>{label}</label><Input id={key} type={type} value={form[key as keyof typeof form]} onChange={(event) => setForm((current) => ({ ...current, [key]: event.target.value }))} placeholder={placeholder} className="mt-2 h-11" /></div>)}{message && <p className="text-sm text-coral">{message}</p>}<Button type="submit" className="h-11 w-full bg-brand text-paper hover:bg-brand">Create account</Button></form><p className="mt-6 text-center text-sm text-ink/60">Already registered? <Link to="/login" className="font-medium text-brand-deep underline underline-offset-4">Log in</Link></p></div></div></div>; }
