import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { ArrowLeft, LockKeyhole } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export const Route = createFileRoute("/login")({ head: () => ({ meta: [{ title: "Log in — RoadWatch" }, { name: "description", content: "Log in to report and track damaged roads." }, { property: "og:title", content: "Log in — RoadWatch" }, { property: "og:description", content: "Log in to report and track damaged roads." }] }), component: LoginPage });
function LoginPage() { const navigate = useNavigate(); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [message, setMessage] = useState(""); const submit = (event: React.FormEvent) => { event.preventDefault(); if (!email || !password) { setMessage("Enter your email and password to continue."); return; } navigate({ to: "/dashboard" }); }; return <div className="grid min-h-screen place-items-center bg-cream px-4 py-10"><div className="w-full max-w-md"><Link to="/" className="mb-8 inline-flex items-center gap-2 font-mono text-[10px] uppercase tracking-[.1em] text-ink/55 hover:text-ink"><ArrowLeft className="size-3.5" />Back to RoadWatch</Link><div className="border-2 border-ink bg-paper p-6 shadow-[6px_6px_0_0_var(--ink)] sm:p-8"><div className="grid size-10 place-items-center rounded-md bg-brand text-paper shadow-[3px_3px_0_0_var(--ink)]"><LockKeyhole className="size-5" /></div><h1 className="mt-5 text-3xl font-semibold tracking-tight">Welcome back.</h1><p className="mt-2 text-sm text-ink/60">Open your reports and keep the neighborhood moving.</p><form onSubmit={submit} className="mt-7 space-y-4"><div><label className="field-label" htmlFor="email">Email</label><Input id="email" type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@example.com" className="mt-2 h-11" /></div><div><label className="field-label" htmlFor="password">Password</label><Input id="password" type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="••••••••" className="mt-2 h-11" /></div>{message && <p className="text-sm text-coral">{message}</p>}<Button type="submit" className="h-11 w-full bg-brand text-paper hover:bg-brand">Log in</Button></form><p className="mt-6 text-center text-sm text-ink/60">New to the field tool? <Link to="/register" className="font-medium text-brand-deep underline underline-offset-4">Create an account</Link></p></div></div></div>; }
